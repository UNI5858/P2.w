#include <stdio.h>
#include <string.h>

int main() {
	char s1[100];
	char s2[100];
	char s3[100];

	printf("입력:");
	scanf_s("%s", s1, sizeof(s1));

	printf("입력:");
	scanf_s("%s", s2, sizeof(s2));

	printf("입력:");
	scanf_s("%s", s2, sizeof(s2));

	if (strcmp(s1, s2) > 0) {
		if 
	}
}